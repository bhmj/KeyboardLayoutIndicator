namespace KeyboardLayoutIndicator.Settings
{
    internal static class DefaultYaml
    {
        public const string Content = @"# ============================================================
#  Настройки индикатора раскладки клавиатуры
# ============================================================
#
#  Структура файла:
#
#    <имя_раскладки>:
#      mode: none | border | overlay   # ничего / рамка / цветовой фильтр на весь экран
#      color: ""R,G,B""                  # цвет рамки/заливки, например ""255,80,0""
#      thickness: 12                    # толщина рамки в пикселях (используется при mode: border)
#      sound: true | false              # звук-щелчок при каждом нажатии клавиши в этой раскладке
#      soundFile: """"                    # необязательно: путь к своему .wav файлу вместо встроенного щелчка
#
#  Имя раскладки это имя языка ввода Windows
#  (en-US, ru-RU, uk-UA, de-DE, fr-FR и прочее).
#
#  Если раскладки нет в файле настроек — используется режим ""ничего""
#  без звука.
#
#  Раздел ""options"" — общие настройки программы:
#    pollIntervalMs      — как часто проверять текущую раскладку, мс
#    disableInFullscreen — отключать рамку/заливку/звук в полноэкранных
#                          приложениях (играх): true / false
#    borderOpacity        — прозрачность рамки, от 0.0 (невидима) до 1.0
#    overlayOpacity       — прозрачность заливки на весь экран, 0.0 - 1.0
#
#  Раздел ""capsLock"" — дополнительный индикатор состояния CapsLock.
#  Работает совместно с индикатором раскладки: если CapsLock включён,
#  его рамка/заливка накладывается поверх уже показанной для текущей
#  раскладки (но не вместо неё). Если раскладка в режиме ""border"",
#  рамка CapsLock рисуется внутри неё, образуя двойную рамку.
#    enabled   — включить индикатор CapsLock: true / false
#    mode      — none | border | overlay
#    color     — ""R,G,B""
#    thickness — толщина рамки в пикселях (используется при mode: border)
#  Прозрачность берётся из тех же borderOpacity / overlayOpacity выше.
#
#  Звук работает только при нажатии клавиш, значение которых зависит от
#  раскладки: буквы и верхний цифровой ряд (включая доп. знаки на нём),
#  а также соседние с ними клавиши пунктуации.
#
#  Файл можно редактировать прямо во время работы программы (через
#  пункт меню трея ""Открыть файл настроек"") — изменения применяются
#  автоматически после сохранения, перезапуск не требуется.
# ============================================================

capsLock:
  enabled: false
  mode: border
  color: ""255,255,255""
  thickness: 6

en-US:
  mode: none
  color: ""0,120,215""
  thickness: 12
  sound: false
  soundFile: """"

ru-RU:
  mode: border
  color: ""255,120,0""
  thickness: 12
  sound: true
  soundFile: """"

options:
  pollIntervalMs: 120
  disableInFullscreen: true
  borderOpacity: 0.55
  overlayOpacity: 0.12
";
    }
}
